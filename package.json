{
  "name": "mt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ksr dev -e index.html -p 8991 -h 0.0.0.0 --static ./",
    "build": "rm -rf dist && npm run build-index && npm run build-main && npm run build-panel",
    "build-index":"ksr build -e index.html:path=test/index.html --min false",
    "build-main":"ksr build -e mt.ts:path=mt --hash false --library mt --target umd",
    "build-panel": "ksr build -e mt-panel.ts:path=mtpanel --hash false --library mtpanel --target umd",
    "fix-lint": "tslint -p tsconfig.json --fix",
    "lint": "tslint -p tsconfig.json -t verbose"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ksr": "^3.8.5",
    "tslint": "^6.1.0",
    "tslint-config-standard": "^9.0.0"
  }
}
