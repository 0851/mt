{
  "name": "mtsdk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ksr dev -e index.html -p 8991 -h 0.0.0.0 --static ./",
    "build": "npm run build-main && npm run build-plugin",
    "build-main":"ksr build -e mtsdk.ts:path=mtsdk --hash false --library mtsdk --target umd",
    "build-panel": "ksr build -e mtsdk-panel.ts:path=mtsdkpanel --hash false --library mtsdkpanel --target umd",
    "fix-lint": "tslint -p tsconfig.json --fix",
    "lint": "tslint -p tsconfig.json -t verbose"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ksr": "^3.8.3",
    "tslint": "^6.1.0",
    "tslint-config-standard": "^9.0.0"
  }
}
